<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GCP Newsletter</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:ital,wght@0,200..1000;1,200..1000&display=swap"
        rel="stylesheet">

    {{!-- Style for email newsletter --}}
    <style>
        body {
            font-family: "Nunito", sans-serif;
            margin: 0;
            padding: 0;
            color: #333;
            background-color: #f5f5f5;
            line-height: 1.6;
        }

        .container {
            max-width: 1360px;
            margin: 0 auto;
            background-color: white;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            min-height: 100vh;
            display: grid;
            grid-template-areas:
                "header header"
                "left right"
                "footer footer"
                "copyright copyright";
            ;
            grid-template-columns: 70% 30%;
            grid-template-rows: auto 1fr auto auto;
        }

        /* Header Styles */
        .header {
            grid-area: header;
            background: linear-gradient(135deg, rgb(0, 0, 134), rgb(251, 164, 157));
            padding: 10px 15px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid #eaeaea;
        }

        .left-column {
            grid-area: left;
            padding: 8px;
        }

        .right-column {
            grid-area: right;
            padding: 8px;
        }

        /* Remove existing float/width declarations */
        .left-column,
        .right-column {
            float: none;
            display: block;
        }

        /* Footer Styles */
        .footer {
            grid-area: footer;
            background: #f8f9fa;
            padding: 20px;
            border-top: 1px solid #eaeaea;
            display: flex;
        }

        /* Rest of your existing styles remain unchanged */
        .header-logo {
            display: flex;
            align-items: center;
        }

        .logo {
            height: auto;
        }

        .newsletter-title {
            font-size: 26px;
            font-weight: bold;
            color: white;
            font-family: "Nunito", sans-serif;
            margin-right: 40px;
        }

        .issue-info {
            font-size: 14px;
            color: white;
            text-align: right;
        }

        .section {
            padding: 8px;
            padding-left: 15px;
            border-bottom: 1px solid #eaeaea;
            border-left: 1px solid #eaeaea;
        }

        .section-title {
            font-size: 18px;
            font-weight: bold;
            margin-bottom: 10px;
            color: #202124;
        }

        /* Leadership Message */
        .leadership-message {
            background-color: #f8f9fb;
            padding: 1px;
            margin-bottom: 10px;
        }

        .leader-info {
            display: flex;
            align-items: center;
            margin-bottom: 5px;
        }

        .leader-avatar {
            width: 35px;
            height: 35px;
            border-radius: 50%;
            margin-right: 10px;
        }

        .leader-name {
            font-weight: bold;
        }

        .leader-quote {
            font-style: italic;
            margin-left: 2px;
            color: #5f6368;
            font-size: small;
        }

        .updates-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 10px;
        }

        .update-card {
            border: 1px solid #eaeaea;
            border-radius: 8px;
            padding: 8px;
            position: relative;
        }

        .update-icon {
            width: 24px;
            height: 24px;
            background-color: #4285f4;
            border-radius: 4px;
            display: flex;
            justify-content: center;
            align-items: center;
            margin-bottom: 10px;
        }

        .update-icon span {
            color: white;
            font-size: 14px;
            font-weight: bold;
        }

        .update-title {
            font-weight: bold;
            margin-bottom: 5px;
        }

        .update-description {
            font-size: 14px;
            color: #5f6368;
            margin-bottom: 10px;
        }

        .learn-more {
            color: #4285f4;
            text-decoration: none;
            font-size: 14px;
            display: flex;
            /* align-items: center; */
        }

        .learn-more:after {
            content: "›";
            margin-left: 5px;
        }

        /* Initiatives */
        .initiatives-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 10px;
        }

        .initiative-card {
            border: 1px solid #eaeaea;
            border-radius: 8px;
            overflow: hidden;
        }

        .initiative-image {
            width: 100%;
            height: 120px;
            background-color: #e8eaed;
            object-fit: cover;
        }

        .initiative-content {
            padding: 8px;
        }

        .initiative-title {
            font-weight: bold;
            margin-bottom: 5px;
        }

        .initiative-description {
            font-size: 14px;
            color: #5f6368;
        }

        /* Security Updates */
        .security-alert {
            background-color: #fdeded;
            border-radius: 8px;
            padding: 8px;
            display: flex;
            align-items: flex-start;
            margin-bottom: 10px;
        }

        .alert-icon {
            color: #ea4335;
            margin-right: 15px;
        }

        .alert-content {
            flex-grow: 1;
        }

        .alert-title {
            font-weight: bold;
            margin-bottom: 5px;
        }

        .alert-description {
            font-size: 14px;
            margin-bottom: 10px;
        }

        .alert-button {
            background-color: #ea4335;
            color: white;
            border: none;
            padding: 6px 8px;
            border-radius: 4px;
            cursor: pointer;
            font-size: 11px;
        }

        /* Technical Insights */
        .tip-container {
            margin-bottom: 10px;
        }

        .tip-header {
            display: flex;
            align-items: center;
            margin-bottom: 10px;
        }

        .tip-icon {
            color: #fbbc04;
            margin-right: 10px;
            font-size: 20px;
        }

        .tip-title {
            font-weight: bold;
        }

        .tip-content {
            font-size: 14px;
            color: #5f6368;
            margin-bottom: 10px;
        }

        /* Learning & Events */
        .events-grid {
            display: grid;
            grid-template-columns: repeat(1, 1fr);
            gap: 10px;
        }

        .event-card {
            border: 1px solid #eaeaea;
            border-radius: 8px;
            padding: 8px;
        }

        .event-icon {
            font-size: 24px;
            margin-bottom: 10px;
            color: #4285f4;
        }

        .training-icon {
            color: #34a853;
        }

        .event-title {
            font-weight: bold;
            margin-bottom: 5px;
        }

        .event-description {
            font-size: 14px;
            color: #5f6368;
            margin-bottom: 10px;
        }

        .event-button {
            border: none;
            padding: 6px 8px;
            border-radius: 4px;
            cursor: pointer;
            font-size: 11px;
            color: white;
            background-color: #4285f4;
        }

        .training-button {
            background-color: #34a853;
        }

        .section {
            max-width: 900px;
            margin: auto;
        }

        .spotlight-container {
            display: grid;
            grid-template-columns: repeat(3, minmax(150px, 1fr));
            /* Ensures max 2 columns */
            gap: 10px;
        }

        .spotlight-card {
            background-color: #f1f8e9;
            border-radius: 8px;
            padding: 8px;
            display: flex;
            /* Change from grid to flex for better alignment */
            /* align-items: center; */
            box-sizing: border-box;
        }

        .spotlight-avatar {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            margin-right: 15px;
        }

        .spotlight-content {
            flex-grow: 1;
        }

        .spotlight-name {
            font-weight: bold;
            margin-bottom: 5px;
        }

        .spotlight-achievement {
            font-size: 14px;
            margin-bottom: 5px;
        }

        /* Responsive Design */
        @media (max-width: 600px) {
            .spotlight-container {
                grid-template-columns: 1fr;
                /* Stack cards in a single column on small screens */
            }
        }


        /* Community & Collaboration */
        .community-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 10px;
        }

        .community-card {
            border: 1px solid #eaeaea;
            border-radius: 8px;
            padding: 8px;
            display: flex;
            justify-content: center;
        }

        .shoutouts-list {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }

        .shoutout-item {
            display: flex;
            align-items: center;
            margin-bottom: 10px;
        }

        .shoutout-team {
            font-weight: bold;
            margin-bottom: 5px;
        }

        .shoutout-contribution {
            font-size: 14px;
            color: #5f6368;
        }

        .ideas-card {
            text-align: center;
        }

        .ideas-title {
            font-weight: bold;
            margin-bottom: 10px;
        }

        .ideas-description {
            font-size: 14px;
            color: #5f6368;
            margin-bottom: 15px;
        }

        .ideas-button {
            background-color: #4285f4;
            color: white;
            border: none;
            padding: 7px 9px;
            border-radius: 4px;
            cursor: pointer;
            font-size: 12px;
        }

        /* Next Month Focus */
        .focus-items {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 10px;
        }

        .focus-item {
            display: flex;
            align-items: flex-start;
        }

        .focus-icon {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            margin-right: 15px;
            color: white;
        }

        .cloud-icon {
            background-color: #4285f4;
        }

        .security-icon {
            background-color: #34a853;
        }

        .focus-content {
            flex-grow: 1;
        }

        .focus-title {
            font-weight: bold;
            margin-bottom: 5px;
        }

        .focus-description {
            font-size: 14px;
            color: #5f6368;
        }

        /* Footer
.footer {
    background-color: #f8f9fa;
    max-width: fit-content;
    padding: 8px;
    display: flex;
    justify-content: space-between;
    align-items: center;
} */

        .feedback-section {
            flex-grow: 1;
        }

        .feedback-title {
            font-size: 16px;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .feedback-description {
            font-size: 14px;
            color: #5f6368;
            margin-bottom: 10px;
        }

        .feedback-button {
            background-color: transparent;
            border: 1px solid #5f6368;
            color: #5f6368;
            padding: 6px 8px;
            border-radius: 4px;
            cursor: pointer;
            font-size: 11px;
        }

        .social-section {
            display: flex;
            align-items: center;
        }

        .social-title {
            font-size: 16px;
            font-weight: bold;
            margin-right: 10px;
        }

        .social-icons {
            display: flex;
            position: relative;
        }

        .social-icon {
            width: 24px;
            height: 24px;
            /* margin-left: 15px; */
            color: #5f6368;
        }

        .copyright {
            grid-area: copyright;
            font-size: 12px;
            color: #5f6368;
            text-align: center;
            display: block;
            left: 0;
            bottom: 0;
            width: 100%;
            padding: 8px;
            background: linear-gradient(135deg, rgb(251, 164, 157), rgb(0, 0, 134));
            color: white;
        }

        .anchor {
            display: inline-block;
        }

        .social-media-icons {
            margin-right: 8px;
            margin-left: 8px;
        }

        .view-more {
            display: inline-block;
            color: #0073e6;
            font-weight: bold;
            text-decoration: none;
            margin-left: 5px;
            transition: color 0.3s ease-in-out;
        }

        /* Responsive Styles */
        @media (max-width: 768px) {

            .updates-grid,
            .initiatives-grid,
            .events-grid,
            .community-grid,
            .focus-items {
                grid-template-columns: 1fr;
            }
        }
    </style>

</head>





<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <div class="header-logo">
                <a href="https://www.bluealtair.com"><img class="logo"
                        src="https://www.bluealtair.com/hubfs/Logo-ColorStar-WhiteText.svg" width="150"></a>
            </div>
            <div class="newsletter-title">GCP Newsletter</div>
            <div class="issue-info">
                Issue #{{issueNumber}}<br>
                {{issuedate}}
            </div>
        </div>

        <div class="left-column">
            <!-- Featured Updates Section -->
            <div class="section">
                <div class="section-title">Featured Updates & Announcements</div>
                <div class="updates-grid">
                    {{#each featuredUpdates}}
                    <div class="update-card">
                        <div class="update-title">{{title}}</div>
                        <div class="update-description">{{description}}</div>
                        <a href="{{link}}" class="learn-more">Learn more</a>
                    </div>
                    {{/each}}
                </div>
            </div>

            <!-- Blue Altair's GCP Initiatives -->
            <div class="section">
                <div class="section-title">Blue Altair's GCP Initiatives</div>
                <div class="initiatives-grid">
                    {{#each initiatives}}
                    <div class="initiative-card">
                        <img src="{{image}}" alt="{{title}}" class="initiative-image">
                        <div class="initiative-content">
                            <div class="initiative-title">{{title}}</div>
                            <div class="initiative-description">{{description}}</div>
                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>


            <!-- Certification Spotlight Section -->
            <div class="section">
                <div class="section-title">Certification Spotlight</div>
                <div class="spotlight-container">
                    {{#each certifications}}
                    <div class="spotlight-card">
                        <img src="{{image}}" alt="{{name}}" class="spotlight-avatar">
                        <div class="spotlight-content">
                            <div class="spotlight-name">{{name}}</div>
                            <div class="spotlight-achievement">{{achievement}}</div>
                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>

            <!-- Community & Collaboration Section -->
            <div class="section">
                <div class="section-title">Community & Collaboration</div>
                <div class="card-title">Shoutouts & Kudos !!</div>
                <div class="community-grid">
                    {{#each shoutouts}}
                    <div class="community-card">
                        <div>
                            <div class="shoutout-team">{{name}}</div>
                            <div class="shoutout-contribution">{{contribution}}</div>
                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>
        </div>

        <div class="right-column">
            <!-- Leadership Message Section -->
            <div class="section">
                <div class="section-title">Leadership Message</div>
                <div class="leadership-message">
                    <div class="leader-info">
                        {{!-- <img src="https://avatars.slack-edge.com/2025-02-10/8440320241681_b1bee1b06a87b9d59ae0_72.png"
                            alt="{{leadershipMessage.name}}" class="leader-avatar"> --}}
                        <div class="leader-name">{{leadershipMessage.name}}</div>
                    </div>
                    <div class="leader-quote">
                        "{{leadershipMessage.content}}"
                    </div>
                </div>
            </div>

            <!-- Security Updates Section -->
            <div class="section">
                <div class="section-title">Security Updates</div>
                <div class="security-alert">
                    <div class="alert-content">
                        <div class="alert-title">{{securityUpdate.title}}</div>
                        <div class="alert-description">{{securityUpdate.description}}</div>
                        <button class="alert-button">View Details</button>
                    </div>
                </div>
            </div>

            <!-- Technical Insights Section -->
            <div class="section">
                <div class="section-title">Technical Insights</div>
                <div class="tip-container">
                    <div class="tip-header">
                        <div class="tip-title">Tip of the Month</div>
                    </div>
                    <div class="tip-content">{{technicalTip}}</div>
                </div>
            </div>

            <!-- Learning & Events Section -->
            <div class="section">
                <div class="section-title">Learning & Events</div>
                <div class="events-grid">
                    {{#each events}}
                    <div class="event-card">
                        <div class="event-title">{{title}}</div>
                        <div class="event-description">{{description}}</div>
                        {{!-- <a href="{{link}}"
                            class="event-button {{#if isTraining}}training-button{{/if}}">{{buttonText}}</a> --}}
                    </div>
                    {{/each}}
                </div>
            </div>

            <!-- Next Month's Focus Section -->
            {{!-- <div class="section">
                <div class="section-title">Next Month's Focus</div>
                <div class="focus-items">
                    {{#each nextMonthFocus}}
                    <div class="focus-item">
                        <div class="focus-content">
                            <div class="focus-title">{{title}}</div>
                            <div class="focus-description">{{description}}</div>
                        </div>
                    </div>
                    {{/each}}
                </div> --}}
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            <div class="feedback-section">
                <div class="feedback-title">Feedback & Suggestions</div>
                <div class="feedback-description">{{feedbackText}}</div>
                <button class="feedback-button">Provide Feedback</button>
                {{!-- <button class="ideas-button">Share Ideas</button> --}}
            </div>
            <div class="social-section">
                <div class="social-title">Stay Connected</div>
                <div class="social-icons">
                    {{#each socialLinks}}
                    <div class="social-media-icons"><a class="anchor" href="{{url}}"><img src="{{image}}"
                                class="social-icon"></a></div>
                    {{/each}}
                </div>
            </div>
        </div>

        <!-- Copyright -->
        <div class="copyright">
            © {{currentYear}} Blue Altair GCP Newsletter. All rights reserved.
        </div>
    </div>
</body> 


</html>